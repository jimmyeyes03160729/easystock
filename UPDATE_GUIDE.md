# 墨衡量化 v2 更新說明

這包包含完整替換檔，不是補丁。保留上一版黑白介面與台股漲紅跌綠。沒有推送 GitHub、部署網站或寫入正式 Firebase。

## 更新方式

1. 解壓縮 `easystock-v2-update.zip`，依下表的相同路徑新增／替換檔案，建議同一次提交；不要只更新 index.html。
2. GitHub → Actions → **Daily Stock Data Update** → **Run workflow**，先產生 v2 的選股資料。第一次完成前，首頁會顯示等待新版資料，不沿用舊版推薦。
3. 隔日衝等待下一個台灣工作日的13:05／13:15／13:42排程；手動執行限13:00–13:50。其他時間不覆寫舊資料。
4. 重新整理網站，確認日線資料日、後端完成時間及隔日衝掃描時間。**更新 GitHub 不會立刻重新計算 Firebase**；需要第2步成功完成。
5. 沿用既有 Firebase / Fugle Secrets；不需把任何密鑰填進檔案。若 Actions 失敗，查看該次執行紀錄，首頁不會用假資料补推薦。

| GitHub 路徑 | 用途 |
|---|---|
| `index.html` | 完整黑白首頁、後端資格判定、過期阻擋、更新時間、每分鐘檢查日線及隔日衝 |
| `strategy_rules.py` | **新增，必須上傳**；日線當前選股與歷史技術訊號共用的規則 |
| `update_market.py` | 欄位原始沿用日期、正規化法人比例、版本化發布、選股快照、回測標示 |
| `scan_intraday.py` | 隔日衝時段、完整K棒、VWAP硬排除、候選補位、觀察快照 |
| `.github/workflows/update.yml` | 16:15主更新、20:15補抓；新增檢查；逾時30分鐘 |
| `.github/workflows/intraday-picks.yml` | 13:05／13:15尾盤候選、13:42收盤研究；最多掃描50檔 |
| `.github/workflows/backfill-fugle-history.yml` | 與日線更新共用鎖，避免同時發布；仍只手動補歷史 |
| `tests/test_parsers.py` | 保留既有解析器測試，修正共用模組載入路徑 |
| `tests/test_selection.py` | 資格、缺值、過期、分K與離線發布回歸測試 |
| `tests/test_frontend.cjs` | 首頁資格、市場狀態、配色、快照一致性回歸測試 |
| `UPDATE_GUIDE.md` | 本說明 |

`.github/workflows/` 裡的三個 yml 檔必須放在該資料夾，放在根目錄不會排程。

## 本次完成

- 不合格股票不再補進今日首選／長期核心；最多三檔、不強迫滿額。
- 今日首選每個已知產業最多一檔；沒有產業資料的不列入該區塊。
- 市場狀態固定以完整、同版資料計算，不受股價或預算影響；樣本不足不判斷方向。
- 短波段的當前資格與歷史技術研究共用 Python 規則；前端直接讀取後端決策，不另造一套分數。
- 排序分不標為勝率；移除未驗證的長期固定70分模型信任加分。
- 長期核心必要資料（包括正FCF）不齊／過期不通過；金融業不套用一般企業負債模型。
- 沿用基本面以欄位自己的 `field_meta.as_of` 判斷期限，更新股價不延長期限；沒有來源日期的舊值不再沿用。
- 法人比例改以有日期配對的買賣超股數／同期成交股數計算，至少10個有效觀察；不宣稱一定已有15個交易日。
- 隔日衝刪除日線短線代理回測的加分；保留的診斷函式已修正百分比單位。
- 超過VWAP 3%、跌破VWAP及特定長上影等採硬排除；當日完整5分K才可使用，15分K按時鐘切齊，最近6根須連續。
- 後端回傳掃描範圍內所有合格隔日衝候選，前端套用預算後才選前三；仍不是全市場500檔的即時掃描。
- 隔日衝跨日不沿用；盤中掃描超過20分鐘不顯示舊候選；收盤版標記研究、不是進場訊號。
- 每60秒檢查日線批次與隔日衝更新；正式Shioaji當沖5秒輪詢與當沖進出場程式沒有改寫。
- 新日線寫入獨立版本後才切換 `active_release`；保留三批完整版本，舊路徑兼容讀取亦檢查批次ID。
- 累積每日選股觀察與隔日衝分K快照；不再因股票離開熱門池就刪除其既有歷史。
- 不再把重疊股票交易串成資金曲線，最大回撤／投組Sharpe標為未建立，保留單筆訊號統計。
- K線日期與摘要不同時不混用；AI通報非今日即不顯示舊分析，09:00後產生則標為盤中更新。

## 更新時間判斷（全部台灣時間）

檢查時間：2026-09-09約15:32。資料是當時讀取的快照，不代表之後沒有更新。

| 項目 | 原本／實際觀察 | 判斷與新版 |
|---|---|---|
| 今日首選、短波段、長期核心 | 同用日線summary，排程15:30；當時資料日9/8、後端9/9 14:18完成 | 日線使用上一交易日資料在盤中是正常的；15:32仍不足以認定15:30工作失敗。原排程缺少補抓，改16:15與20:15。 |
| 隔日衝 | 原排程12:00、12:30、13:00、13:20；最新掃描13:29:55，session仍為intraday | 結果太靠近收盤，且沒有明確收盤後確認版。改13:05、13:15、13:42；實際執行超時或K棒太舊拒絕發布。 |
| AI盤前通報 | generated_at是9/9 11:45:29 | 這是盤中，不是盤前。產生程式／排程不在此repo，**未修正外部產生時間**；首頁已如實標示。真正盤前應在09:00前完成。 |
| 3／5年歷史補抓 | workflow_dispatch | 本來就是手動，一般日常不用重跑；不需要每分鐘更新。 |
| 月營收、季報、股利 | 每次日線作業重新抓官方最新資料 | 按資料公布週期改變，不會每天都有新營收或季報。抓取時間與財報所屬期間不可混同。 |

GitHub Actions cron 是「觸發時間」，不是保證完成時間。官方明示高負載時會延遲，排程也可能被丟棄。改成不在整點與縮短尾盤工作只能降低風險，不能保證準點；若要可交易的準時尾盤訊號，應把隔日衝移到既有常駐主機的排程。這包沒有更動你的Oracle VM。

GitHub公開執行紀錄API當時回傳403速率限制，因此無法核實每次工作究竟排隊多久；13:29:55是Firebase記錄的掃描完成時間，不能假稱它一定是哪一筆cron觸發。

## 新規則邊界與尚待資料支持的項目

本版是保守規則修復與驗證基礎，不是已證明更賺錢的新模型。

- 初始日線門檻：排序至少70分、成交金額至少5,000萬元；短波段共用多頭排列、量能、乖離檢查。強勢波段需250根日K、近高比至少0.90、Sharpe至少0.5、量比0.8–4、20日線乖離不超過12%；回踩需60根日K、近3日低點距均線-3%至+2%、量比0.8–3、20日線乖離不超過6%。這些是版本化研究門檻，沒有宣稱已最適化。
- 長期部分仍是「收益型觀察」：正ROE/EPS/FCF、負債比<70%、正本益比、殖利率≥4%、配息紀錄≥3年，且必要欄位有有效日期。**本次讀到500檔FCF全缺，因此長期區塊可能全空，這是正常的嚴格篩選結果。** 本版沒有杜撰FCF，也沒有新增不存在的多季歷史、金融業專用模型或完整配息可持續性模型。
- `field_meta.as_of` 對目前API新抓欄位代表觀察日期，`published_at` 未知就留空；不把觀察日假裝成財報公布日。原始財报年季完整性與多季公司品質比較仍需補來源。
- 日線日期與每檔摘要必須一致；前端7個日曆日是長假容忍的最高上限，不是交易所行事曆判定。未接官方完整休市日表；仍必須看畫面所示資料日。隔日衝在休市日因没有當日有效分K不發布。
- 回測仍是「日線技術訊號研究」，不是今天實際TOP3投組，也不是長期或隔夜策略績效。仍存在目前股票池偏誤、公司行動／還原價格處理不完整、理想開收盤成交假設；不可把既有統計解讀為可以實現的績效。
- 專用隔夜回測、可成交性（漲跌停／處置／暫停交易／滑價／跳空）、完整資金配置、產業相對估值／相對大盤強弱，仍需對應資料與持續樣本。已開始保留快照，但尚未憑不足資料宣稱完成這些模型。
- Firebase新增 `releases`、`active_release`、`selection_history`、`overnight_history`。前兩項供新版前端讀取，後兩項供後端研究；研究快照會持續成長。沿用root讀取權限時可走既有summary/meta；若你的線上規則逐節點白名單且遇到PERMISSION_DENIED，請提供實際規則再合併，**不要直接用repo裡舊firebase.rules.json覆蓋線上設定**。
- 保留三批完整日線版本；每日決策與隔日衝研究快照另外累積，需按Firebase實際容量定期備份／歸檔。

## 驗證

- 7項原有解析器測試。
- 13項選股／時效／分K／成本統計／離線發布回歸測試。
- Node前端檢查：不補名單、市場狀態不受預算改變、產業分散、舊日隔日衝阻擋、黑白K線、漲紅跌綠、數字與資料批次驗證。
- Python/JavaScript語法、工作流程YAML重複鍵、HTML結構與git空白檢查。
- 沒有執行真實下單、正式Firebase寫入、線上部署或瀏覽器視覺測試。測試不代表投資收益驗證。

## 查核來源

- https://easystock-c237a-default-rtdb.firebaseio.com/market_data/meta.json
- https://easystock-c237a-default-rtdb.firebaseio.com/market_data/intraday_picks.json
- https://easystock-c237a-default-rtdb.firebaseio.com/market_data/premarket_brief.json
- https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule
- https://www.twse.com.tw/zh/products/system/trading.html

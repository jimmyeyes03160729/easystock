# EasyStock 專案開發與協作規則

本文件為 EasyStock 專案在 Antigravity 環境下的常駐開發指令規則。所有在此專案進行的分析、開發、重構、測試與維運皆須嚴格遵循以下規範。

---

## 1. 語言與溝通規範
* **主要語言**：所有回答、專案架構分析、執行計畫、程式修改說明、問題排查及測試報告，必須全程使用**繁體中文（台灣習慣用語）**。
* **原文保留**：
  * 程式碼（變數名、函式、類別、屬性）。
  * 檔案路徑與目錄結構。
  * Git 指令與各項 CLI 工具命令。
  * 必要且通用的技術術語（如：Tick, Order, WebSocket, Workflow, Webhook, API, OOS, VWAP, Docker, Systemd 等）。

---

## 2. 授權與執行模式（全自動最高權限）
使用者已明確授予全面自主執行權限，以下操作**無須每次事前詢問確認，可直接自動執行**：
1. **GitHub 與版本控制**：程式碼修改完成後，可直接執行 `git add`、`git commit` 與 `git push`（包含推送到遠端分支）。
2. **Oracle VM 伺服器操作**：擁有最高操作權限，可直接透過 SSH 執行指令、檢查服務狀態、更新伺服器檔案、管理或重啟 systemd 服務。
3. **檔案管理與開發**：專案開發所需的檔案增修、重構、測試與維護可直接進行，不需每步反覆確認。
4. **GitHub Actions CI 自動閉環監控與主動修復（重要）**：
   * 每次執行 `git push` 後，**必須主動使用 `gh run` 追蹤 Actions 流程狀態至完成**。
   * 若 Actions 未通過（紅燈），**嚴格禁止等待使用者到 GitHub 查看並手動貼錯誤**，必須立即自動讀取失敗日誌、定位根本原因、完成程式修正並再次推送，直到 CI 測試全數綠燈通過。

---

## 3. EasyStock 專案核心防線
* **純模擬交易邊界**：
  * 盤中當沖與研究引擎僅處理虛擬模擬帳戶（Paper Trade）。
  * 嚴格禁止呼叫或串接真實券商下單委託 API，不得擅自開啟真實交易旗標。
* **機密資訊保護**：
  * 嚴格禁止將券商金鑰、Telegram Bot Token、LINE Channel Token、Firebase Service Account 憑證、OAuth 密鑰或任何 `.env` 檔案內容寫入公開程式庫或記錄至公開日誌中。
* **資料與架構合規**：
  * 遵循現行資料契約：日線發布走不可變批次機制（`/market_data/releases/`），不直接破壞活躍發布版本。
  * 盤中狀態維護於 `/market_data/intraday_live`，避免對資料庫進行每 Tick 頻繁寫入。
